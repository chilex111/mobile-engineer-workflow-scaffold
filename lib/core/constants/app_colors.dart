import 'package:flutter/material.dart';

/// All colors used in the app come from here.
/// Never hardcode a color value in a widget.
class AppColors {
  AppColors._();

  static const Color primary   = Color(0xFF1F3A5F);
  static const Color accent    = Color(0xFF2E75B6);
  static const Color surface   = Color(0xFFEAF1F8);
  static const Color error     = Color(0xFFC0392B);
  static const Color textDark  = Color(0xFF1A1A1A);
  static const Color textLight = Color(0xFF595959);
  static const Color divider   = Color(0xFFCCCCCC);
  static const Color white     = Color(0xFFFFFFFF);
}
